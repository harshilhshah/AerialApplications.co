= form_for @project_status_type do |f|
  - if @project_status_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@project_status_type.errors.count, "error")} prohibited this project_status_type from being saved:"
      %ul
        - @project_status_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
