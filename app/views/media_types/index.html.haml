%h1 Listing media_types

%table
  %thead
    %tr
      %th Description
      %th
      %th
      %th

  %tbody
    - @media_types.each do |media_type|
      %tr
        %td= media_type.description
        %td= link_to 'Show', media_type
        %td= link_to 'Edit', edit_media_type_path(media_type)
        %td= link_to 'Destroy', media_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Media type', new_media_type_path
